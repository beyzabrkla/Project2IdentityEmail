<!DOCTYPE html>
<html dir="ltr">

@await Component.InvokeAsync("LoginLayoutHead")

<body>
    <div class="main-wrapper">
        <div class="preloader">
            <div class="lds-ripple">
                <div class="lds-pos"></div>
                <div class="lds-pos"></div>
            </div>
        </div>

        @RenderBody()
    </div>

    @await Component.InvokeAsync("LoginLayoutScripts")

    @* Sayfa özelinde (Register/Login) ek scriptler gelirse buraya basılır *@
    @RenderSection("Scripts", required: false)
</body>
</html>